import { defineConfig } from "vite";

export default defineConfig({
  root: "src",
  build: {
    outDir: "../dist/",
  },
});

// tanda .. adalah folder dist naik satu level dan akan berada dalam root saat pembuatan
// belajar sampai 36:21
